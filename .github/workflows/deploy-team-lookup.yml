# Builds team-lookup with secrets from GitHub Actions, deploys static site to GitHub Pages.
#
# Setup (once per repo):
# 1. Repo → Settings → Pages → Source: GitHub Actions.
# 2. Repo → Settings → Secrets and variables → Actions → Repository secrets
#    Create ONE secret per variable (you cannot paste a whole .env into a single secret).
#    Required: VITE_SUPABASE_URL plus EITHER VITE_SUPABASE_PUBLISHABLE_KEY OR VITE_SUPABASE_ANON_KEY.
#    Optional: VITE_SUPABASE_TEAM_TABLE. Other copy lives in team-lookup/src/App.tsx (SITE_COPY).
# 3. For a project site (https://USER.github.io/REPO/), VITE_BASE_PATH uses REPO name below.
#    For a root site (custom domain or USER.github.io repo), change VITE_BASE_PATH to /

name: Deploy team-lookup to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "team-lookup/**"
      - ".github/workflows/deploy-team-lookup.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-team-lookup
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: team-lookup/package-lock.json

      - name: Install and build
        working-directory: team-lookup
        env:
          # Must match your Pages URL path (project site = /repository-name/)
          VITE_BASE_PATH: /${{ github.event.repository.name }}/
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          VITE_SUPABASE_TEAM_TABLE: ${{ secrets.VITE_SUPABASE_TEAM_TABLE }}
        run: |
          npm ci
          npm run build
          cp dist/index.html dist/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: team-lookup/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
